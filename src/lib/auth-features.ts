/**
 * Phone OTP login / account link.
 * Keep false while BoostSMS sender is pending approval.
 * Flip to true when sender is approved — UI + APIs are already wired.
 */
export const PHONE_AUTH_ENABLED = false;
