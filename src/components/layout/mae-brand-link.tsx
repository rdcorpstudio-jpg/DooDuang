"use client";

/**
 * Brand mark — disabled site-wide (โลโก้/ชื่อแบรนด์มุมบนเอาออกทุกหน้า).
 * Keep the export so existing imports compile without layout churn.
 */
export function MaeBrandLink(_props?: {
  href?: string;
  className?: string;
}) {
  return null;
}
